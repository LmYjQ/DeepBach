# 简谱 DeepBach 三脚本执行说明

本手册覆盖完整的简谱数据训练生成流程，由三个脚本组成：

| # | 脚本 | 作用 |
|---|------|------|
| 1 | `preprocess_chinese_score_batch.py` | 批量将简谱 JSON 转换为 DeepBach 所需的 PT 张量 |
| 2 | `train_simple_notation.py` | 加载 PT 数据，训练 DeepBach（单声部）模型 |
| 3 | `generate_constrained.py` | 读取 JSON 中的 `ban=1` / `yan=1` 标记作为固定音位，加载训练好的模型进行约束生成，输出 MIDI |

下文以 `D:/code/music/sizhu_data/data/tang`（唐谱，8 个 JSON 简谱文件）为例说明执行流程。

---

## 标准流程

> 本节给出可直接复制运行的 PowerShell 命令序列。完成下面三步即可从原始 JSON 得到一段带约束的生成 MIDI。
>
> **本项目使用独立虚拟环境**：`D:\code\music\deepbach_learn\code\.venv`（Windows）。所有 `python` 命令均应指向该 venv 下的解释器，**不要**用系统 Python。

### 0. 准备

```powershell
# 进入代码目录
cd D:\code\music\deepbach_learn\code

# 使用项目自带 venv
$PY = "D:\code\music\deepbach_learn\code\.venv\Scripts\python.exe"

# 输入数据：唐谱
$INPUT_DIR  = 'D:/code/music/sizhu_data/data/tang'

# 输出位置
$PT_DIR     = "$PWD\preprocessed_data"
$MODEL_DIR  = "$PWD\models"
$OUT_DIR    = "$PWD\generated_scores"

# 输入数据中任选一份作为生成约束来源（这里以"汤·云庆"为例）
$GEN_JSON   = "$INPUT_DIR\汤·云庆.json"

# 预处理和训练保持统一精度
$SEQ_SIZE   = 8     # 序列长度（拍）
$SUBDIV     = 8     # 每拍 tick 数（8 = 32 分音符精度）

New-Item -ItemType Directory -Force -Path $PT_DIR, $MODEL_DIR, $OUT_DIR | Out-Null
```

### 1. 预处理 — `preprocess_chinese_score_batch.py`

将文件夹下所有 JSON 合并为一份全局映射的张量数据，输出到 `preprocessed_data/dataset_tang_combined_tensor_dataset.pt`。

```powershell
& $PY .\preprocess_chinese_score_batch.py `
  --input    "$INPUT_DIR" `
  --output   "$PT_DIR" `
  --sequence_size $SEQ_SIZE `
  --subdivision  $SUBDIV
```

**关键参数**：

| 参数 | 默认 | 说明 |
|------|------|------|
| `--input / -i` | `D:/code/music/qmx_reader/dataset_final8` | 输入 JSON 文件夹 |
| `--output / -o` | `./preprocessed_data` | 输出目录 |
| `--sequence_size / -s` | 8 | 序列长度（拍） |
| `--subdivision / -d` | 8 | 每拍 tick 数（建议 8，支持 0.125 拍精度） |
| `--suffix` | 从文件夹名自动取 | 输出文件后缀，影响 `.pt` 文件名 |

**预期输出**：
```
preprocessed_data/dataset_tang_combined_tensor_dataset.pt
```
日志会打印：发现 N 个 JSON 文件、全局音符表大小、各文件样本数。

### 2. 训练 — `train_simple_notation.py`

加载第 1 步生成的 `.pt`，训练一个简谱 DeepBach（单声部）模型。

```powershell
# 模型参数（较小配置，便于快速验证）
$NE = 50   # note_embedding_dim
$ME = 25   # meta_embedding_dim
$LH = 128  # lstm_hidden_size
$LI = 128  # linear_hidden_size
$LD = 0.5  # dropout_lstm
$LL = 2    # num_layers

& $PY .\train_simple_notation.py `
  --data   "$PT_DIR\dataset_tang_combined_tensor_dataset.pt" `
  --subdivision $SUBDIV `
  --note_embedding_dim   $NE `
  --meta_embedding_dim   $ME `
  --num_layers           $LL `
  --lstm_hidden_size     $LH `
  --linear_hidden_size   $LI `
  --dropout_lstm         $LD `
  --train   30 `
  --batch_size 16 `
  --models_dir "$MODEL_DIR"
```

**关键参数**：

| 参数 | 默认 | 说明 |
|------|------|------|
| `--data / -d` | **必填** | 第 1 步产出的 `.pt` 文件路径 |
| `--subdivision / -s` | 8 | 必须与预处理一致 |
| `--note_embedding_dim` | 100 | 音符嵌入维度 |
| `--meta_embedding_dim` | 50 | 元数据嵌入维度 |
| `--num_layers` | 2 | LSTM 层数 |
| `--lstm_hidden_size` | 200 | LSTM 隐藏层大小 |
| `--linear_hidden_size` | 200 | 线性层隐藏大小 |
| `--dropout_lstm` | 0.2 | LSTM dropout |
| `--train / -t` | 20 | 训练轮数 |
| `--batch_size / -b` | 16 | 批次大小 |
| `--models_dir` | `models` | 模型保存目录 |

**预期输出**：模型文件名由参数自动编码，格式如下：
```
models/voicemodel_tang_combined_tensor_dataset_ne50_me25_lh128_ll2_ld0.5_li128_0.pt
```
记下这个文件路径，第 3 步会用到。

### 3. 生成 — `generate_constrained.py`

读取 `$GEN_JSON`，把 `ban=1` / `yan=1` / 整数拍起点的音符当作固定音位，加载第 2 步的模型进行 Gibbs 采样，输出 MIDI。

```powershell
$MODEL = "$MODEL_DIR\voicemodel_tang_combined_tensor_dataset_ne${NE}_me${ME}_lh${LH}_ll${LL}_ld${LD}_li${LI}_0.pt"

& $PY .\generate_constrained.py `
  --json   "$GEN_JSON" `
  --data   "$PT_DIR\dataset_tang_combined_tensor_dataset.pt" `
  --model  "$MODEL" `
  --subdivision $SUBDIV `
  --output  "$OUT_DIR\tang_yunqing.mid" `
  --num_iterations 1000 `
  --temperature     1.0 `
  --batch_size      8 `
  --png
```

**关键参数**：

| 参数 | 默认 | 说明 |
|------|------|------|
| `--json / -j` | **必填** | 输入简谱 JSON（含 `ban` / `yan` 字段） |
| `--data / -d` | **必填** | 第 1 步产出的 `.pt`（用于加载 `note2index`、模型架构） |
| `--model / -m` | **必填** | 第 2 步训练得到的 `.pt` 模型文件 |
| `--subdivision / -s` | 8 | 必须与训练一致 |
| `--output / -o` | `generated_constrained.mid` | 输出 MIDI 路径 |
| `--num_iterations / -n` | 500 | Gibbs 采样迭代次数（越大越精细） |
| `--temperature / -t` | 1.0 | 采样温度（高温=多样，低温=确定） |
| `--batch_size / -b` | 8 | 每轮采样批大小 |
| `--png` | 否 | 同时输出 PNG 简谱预览 |

> **注意**：脚本从模型文件名解析 `note_embedding_dim / meta_embedding_dim / lstm_hidden_size / num_layers / dropout_lstm / linear_hidden_size`，**不要**在命令行再传这些参数，必须保证它们和训练时一致。

**预期输出**：
```
$OUT_DIR\tang_yunqing.mid         # MIDI 文件
$OUT_DIR\tang_yunqing.png         # 简谱预览（若加 --png）
```
日志会逐个打印固定音位的 `tick / beat / value / octave / reason`，生成后再逐个校验这些位置是否保持原值。

### 完整三步一览

```powershell
# === 0. 准备 ===
cd D:\code\music\deepbach_learn\code
$PY        = "D:\code\music\deepbach_learn\code\.venv\Scripts\python.exe"
$INPUT_DIR = 'D:/code/music/sizhu_data/data/tang'
$PT_DIR    = "$PWD\preprocessed_data"
$MODEL_DIR = "$PWD\models"
$OUT_DIR   = "$PWD\generated_scores"
$GEN_JSON  = "$INPUT_DIR\汤·云庆.json"
$SEQ_SIZE  = 8; $SUBDIV = 8
$NE=50; $ME=25; $LH=128; $LI=128; $LD=0.5; $LL=2
New-Item -ItemType Directory -Force -Path $PT_DIR, $MODEL_DIR, $OUT_DIR | Out-Null

# === 1. 预处理 ===
& $PY .\preprocess_chinese_score_batch.py `
  --input "$INPUT_DIR" --output "$PT_DIR" `
  --sequence_size $SEQ_SIZE --subdivision $SUBDIV

# === 2. 训练 ===
& $PY .\train_simple_notation.py `
  --data "$PT_DIR\dataset_tang_combined_tensor_dataset.pt" `
  --subdivision $SUBDIV `
  --note_embedding_dim $NE --meta_embedding_dim $ME `
  --num_layers $LL --lstm_hidden_size $LH `
  --linear_hidden_size $LI --dropout_lstm $LD `
  --train 30 --batch_size 16 --models_dir "$MODEL_DIR"

# === 3. 生成 ===
$MODEL = "$MODEL_DIR\voicemodel_tang_combined_tensor_dataset_ne${NE}_me${ME}_lh${LH}_ll${LL}_ld${LD}_li${LI}_0.pt"
& $PY .\generate_constrained.py `
  --json "$GEN_JSON" `
  --data "$PT_DIR\dataset_tang_combined_tensor_dataset.pt" `
  --model "$MODEL" `
  --subdivision $SUBDIV `
  --output "$OUT_DIR\tang_yunqing.mid" `
  --num_iterations 1000 --temperature 1.0 --batch_size 8 --png
```

---

## 数据约定（tang 示例）

`D:/code/music/sizhu_data/data/tang` 目录下当前包含 8 个简谱 JSON：

```
汤·中花六板.json
汤·云庆.json
汤·凡亡工.json
汤·四合如意.json
汤·欢乐歌.json
汤·老六板.json
汤·花三六.json
汤·行街.json
```

每个 JSON 的音符字段：

```json
{
  "value":    "1"~"7",   // 简谱数字；"0" = 休止符
  "octave":    0,        // 八度偏移
  "duration":  1.0,      // 时值（拍）
  "ban":       0 or 1,   // 板音（约束生成中作为固定点）
  "yan":       0 or 1    // 眼位（约束生成中作为固定点）
}
```

> 预处理时会自动跳过 `value: "bar"`（小节线）和 `value: null` 的条目。

---

## 常见问题

### Q1. 训练时报 `model_suffix` 相关错误？
模型文件名是按 `<data_name_without_ext>` 的后半段编码的，确保 `.pt` 文件名包含 `tang_combined_tensor_dataset` 等可识别后缀。

### Q2. 生成时报"无法从模型文件名解析参数"？
模型文件名必须形如：
```
voicemodel_<suffix>_ne{n}_me{m}_lh{h}_ll{l}_ld{d}_li{i}_{voice}.pt
```
检查训练是否正常完成并产出符合该格式的文件。

### Q3. 训练/生成的 `--subdivision` 必须保持一致？
是的。预处理、训练、生成三者必须使用同一个 `subdivision`（推荐 8），否则 `note2index` 和 tick 对齐都会出错。

### Q4. 想要更长的乐谱 / 更高质量？
- 增加 `--train`（训练轮数）
- 增加 `--num_iterations`（Gibbs 采样次数）
- 减小 `--temperature`（如 0.8 → 更保守）
- 使用更大模型（增大 `ne / me / lh / li`）

### Q5. 想要给多首曲子生成？
只需把第 3 步的 `--json` 与 `--output` 改为其他 JSON 路径与目标文件，对模型和 `.pt` 数据无需重训。

---

## 命令汇总（速查）

```powershell
$PY = "D:\code\music\deepbach_learn\code\.venv\Scripts\python.exe"

# 1. 预处理
& $PY .\preprocess_chinese_score_batch.py `
  --input 'D:/code/music/sizhu_data/data/tang' `
  --output .\preprocessed_data `
  --sequence_size 8 --subdivision 8

# 2. 训练
& $PY .\train_simple_notation.py `
  --data .\preprocessed_data\dataset_tang_combined_tensor_dataset.pt `
  --subdivision 8 `
  --note_embedding_dim 50 --meta_embedding_dim 25 `
  --num_layers 2 --lstm_hidden_size 128 `
  --linear_hidden_size 128 --dropout_lstm 0.5 `
  --train 30 --batch_size 16 `
  --models_dir .\models

# 3. 生成（带 PNG 预览）
& $PY .\generate_constrained.py `
  --json 'D:/code/music/sizhu_data/data/tang\汤·云庆.json' `
  --data .\preprocessed_data\dataset_tang_combined_tensor_dataset.pt `
  --model .\models\voicemodel_tang_combined_tensor_dataset_ne50_me25_lh128_ll2_ld0.5_li128_0.pt `
  --subdivision 8 `
  --output .\generated_scores\tang_yunqing.mid `
  --num_iterations 1000 --temperature 1.0 --batch_size 8 --png
```